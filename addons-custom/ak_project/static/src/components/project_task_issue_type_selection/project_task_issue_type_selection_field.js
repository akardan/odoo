/** @odoo-module */

/*
import { SelectionField } from '@web/views/fields/selection/selection_field';
import { registry } from '@web/core/registry';

export class ProjectTaskIssueTypeSelectionField extends SelectionField {
    static template = 'ak_project.ProjectTaskIssueTypeSelectionField';
    // SelectionField already defines its static props.
    // We typically only redefine props here if we are adding new ones
    // or overriding them with different validation.
    // For now, let's assume props are correctly inherited.

    setup() {
        super.setup();
    }

    get currentValue() {
        return this.props.value || (this.options && this.options.length ? this.options[0][0] : undefined);
    }
}

registry.category('fields').add('selection_issue_type', ProjectTaskIssueTypeSelectionField);
*/
