<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_form_view_tender" model="ir.ui.view">
        <field name="name">product.template.form.tender</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='purchase']/group[@name='purchase']/group[@name='bill']" position="after">
                <group string="İhale Bilgileri" name="tender_info">
                    <field name="manufacturer_id"/>
                    <field name="is_hotel_accommodation"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- Extend Supplier Info Tree View to add custom fields as optional columns -->
    <record id="product_supplierinfo_tree_view_tender" model="ir.ui.view">
        <field name="name">product.supplierinfo.list.view.tender</field>
        <field name="model">product.supplierinfo</field>
        <field name="inherit_id" ref="product.product_supplierinfo_tree_view"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="partner_type" optional="show"/>
                <field name="is_approved" optional="show"/>
            </field>
        </field>
    </record>
</odoo>