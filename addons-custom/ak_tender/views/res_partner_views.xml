<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add fields to list view as optional hide -->
    <record id="view_partner_tree_inherit_ak_tender" model="ir.ui.view">
        <field name="name">res.partner.tree.inherit.ak.tender</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <field name="category_id" position="after">
                <field name="is_hotel" optional="hide"/>
                <field name="hotel_star_rating" widget="priority" optional="hide"/>
            </field>
        </field>
    </record>

    <!-- Add fields to form view in Sales & Purchases sheet, Purchase group -->
    <record id="view_partner_form_inherit_ak_tender" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.ak.tender</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='sales_purchases']//group[@name='purchase']" position="inside">
                <field name="is_hotel"/>
                <field name="hotel_star_rating" widget="priority"/>
            </xpath>
        </field>
    </record>
</odoo>